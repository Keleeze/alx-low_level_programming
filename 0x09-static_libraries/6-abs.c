#include "main.h"

/**
 *  _abs - compares the absolute value of an integer
 *  @n: the int tho check
 *  Return: the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
